{
  "name": "api-for-getting-transaction-eth-polygon",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  },
  "dependencies": {
    "@serverless-stack/cli": "0.60.4",
    "@serverless-stack/resources": "0.60.4",
    "aws-cdk-lib": "2.7.0",
    "moralis": "^1.1.0",
    "node-fetch": "^3.2.0"
  }
}
